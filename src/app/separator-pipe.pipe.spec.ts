import { SeparatorPipePipe } from './separator-pipe.pipe';

describe('SeparatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SeparatorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
